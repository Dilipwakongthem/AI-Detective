import React from 'react';
import { createRoot } from 'react-dom/client';
import DetectiveGame from './components/DetectiveGame';

const root = createRoot(document.getElementById('root'));
root.render(<DetectiveGame />);
